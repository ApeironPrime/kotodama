-r requirements.txt

# Added only once A2–A7 begin: alignment experiments, calibration, API serving
# and fine-tuning. Keeping these out of A0–A1 avoids pulling a CPU-only Torch
# build before the correct CUDA wheel is selected.
accelerate>=1.3,<2
librosa>=0.10,<1
pandas>=2.2,<3
pyarrow>=18,<24
scikit-learn>=1.6,<2
joblib>=1.4,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
python-multipart>=0.0.20,<1
