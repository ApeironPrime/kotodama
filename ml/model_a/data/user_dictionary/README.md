# Optional OpenJTalk user dictionary

Place a compiled OpenJTalk user dictionary (`.dic`) in this directory when a
name, loanword or domain term needs an explicit Japanese reading.  Pass its
path to `--user-dictionary` in the smoke test.

Do not commit a dictionary containing private learner names.
