{
  "name": "kotodama-pronunciation-research-collector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.12.0 <26" },
  "scripts": {
    "dev": "node --env-file-if-exists=.env server/index.mjs",
    "db:migrate": "node --env-file-if-exists=.env server/migrate.mjs",
    "check": "node --check server/index.mjs && node --check server/migrate.mjs"
  },
  "dependencies": { "pg": "^8.23.0" }
}
